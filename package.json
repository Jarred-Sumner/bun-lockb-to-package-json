{
  "name": "bun.lockb2repo",
  "version": "0.0.1",
  "description": "Convert bun.lockb to a repository with package.json, and workspace package.json files if present",
  "license": "MIT",
  "main": "bun.lockb2repo.ts",
  "bin": "bun.lockb2repo.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "files": [
    "bun.lockb2repo.ts"
  ]
}
